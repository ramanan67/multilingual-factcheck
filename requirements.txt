fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
feedparser==6.0.11
beautifulsoup4==4.12.3
lxml==5.3.0
python-dotenv==1.0.1
apscheduler==3.10.4
streamlit==1.38.0
requests==2.32.3
python-multipart==0.0.9

# --- Vector store / ML stack ---
# NOTE ON PYTHON 3.14: chromadb currently has an open, unresolved upstream
# bug on Python 3.14 (chroma-core/chroma#5996, #5983) caused by its
# dependency on Pydantic v1's compatibility shim, which breaks under 3.14.
# Until that's patched upstream, run this project on Python 3.12 (see
# Dockerfile). All application code in this repo is written using only
# 3.14-compatible syntax, so switching is a one-line Dockerfile change
# once chromadb ships a fix -- track it at the issue links above.
chromadb==0.5.5
sentence-transformers==3.0.1
transformers==4.44.2
torch==2.4.1

# --- Photo / video claim ingestion ---
pillow==10.4.0
pytesseract==0.3.13
opencv-python-headless==4.10.0.84
