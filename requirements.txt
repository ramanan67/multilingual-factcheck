fastapi>=0.115.0
uvicorn[standard]>=0.30.6
httpx>=0.27.2
feedparser>=6.0.11
beautifulsoup4>=4.12.3
lxml>=5.3.0

# chromadb: KNOWN RISK on Python 3.14 as of writing. Pin as high as
# possible -- pydantic 2.12+ is required for 3.14 compatibility, and
# chromadb's onnxruntime dependency may not yet ship 3.14 wheels on
# every platform. If `pip install` fails here, see the README's
# "Python 3.14 compatibility note" section for the fallback.
pydantic>=2.12.0
chromadb>=1.5.9

# torch added real Python 3.14 support in the 2.10 release; stay at or
# above that.
torch>=2.10.0
sentence-transformers>=3.0.1
transformers>=4.44.2

python-dotenv>=1.0.1
apscheduler>=3.10.4
streamlit>=1.38.0
requests>=2.32.3
